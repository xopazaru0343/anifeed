query UserOngoingAnime($userName: String, $status: MediaListStatus) {
MediaListCollection(userName: $userName, type: ANIME, status: $status) {
    lists {
      entries {
        media {
          id
          title {
            romaji
            english
          }
          episodes
          status
        }
      }
    }
  }
}